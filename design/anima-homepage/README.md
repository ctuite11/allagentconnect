Anima homepage export.

This folder contains the React + Tailwind export generated from Anima based on the Figma homepage design.

Purpose:
This export is used as a visual layout reference for rebuilding the homepage inside the main application.

The files in /src show the structure, spacing, and component layout from the design.

Do not run this project separately.
Use it only as a blueprint for implementation in the existing codebase.
