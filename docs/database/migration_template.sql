-- ============================================================
-- Migration Template for AAC
-- ============================================================
-- Copy this file into supabase/migrations/ with the naming convention:
--   YYYYMMDDHHMM_description.sql
--
-- Purpose:   [Describe what this migration does]
-- Author:    [Your name]
-- Date:      [YYYY-MM-DD]
-- ============================================================
-- IMPORTANT: All migrations must be safe and additive.
-- - Use CREATE TABLE IF NOT EXISTS
-- - Use ALTER TABLE ... ADD COLUMN IF NOT EXISTS
-- - Use CREATE INDEX IF NOT EXISTS
-- - Never DROP tables or columns without a rollback plan
-- ============================================================

-- === ADD YOUR MIGRATION SQL BELOW ===



-- === ROLLBACK NOTES ===
-- Document how to reverse this migration if needed.
-- Example:
--   ALTER TABLE public.some_table DROP COLUMN IF EXISTS new_column;
--   DROP INDEX IF EXISTS idx_some_table_new_column;
-- ============================================================
