## Plan

1. Reproduce the current mobile failure with Playwright at the reported mobile size and run an `elementFromPoint` check at the Message button center.
2. Identify whether the tap is being intercepted by another layer or whether the dialog trigger is not firing.
3. Patch `src/pages/AgentProfile.tsx` only:
   - keep the action area above overlapping mobile UI with a stronger stacking context if needed;
   - make the guest/public Message trigger a plain, guaranteed clickable dialog trigger;
   - keep authenticated users on the existing conversation-start flow.
4. Verify on mobile that tapping Message opens the contact dialog for guests/public profile views.
5. Verify desktop behavior is unchanged.

## Technical notes

- The previous `relative z-10` may still lose to fixed overlays like the cookie banner (`z-50`) or may not cover the actual trigger wrapper generated by the dialog.
- If `elementFromPoint` shows an overlay, I’ll raise only the Message action region high enough to receive taps.
- If the element is reached but the dialog does not open, I’ll adjust the trigger rendering so Radix receives a direct button element without conflicting props.