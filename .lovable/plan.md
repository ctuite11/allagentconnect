Change the hero headline on the homepage (HomepageV2 → HeroSection) from:

"See the market before it happens."

to:

"The private agent network where agents share pre-market intelligence."

File: `src/components/home-v2/HeroSection.tsx` (line 57) — text-only edit inside the existing `<h1>`. No other files, styles, sub-headline, eyebrow, CTA, or SEO title touched.