Update the subheadline in the founder invite email.

File: `supabase/functions/send-bulk-email/index.ts`

Replace "before public launch" with "before the public feeds" in the subheadline, so it reads:

"Become a Massachusetts Founding Partner and get access to private listings, active buyer demand, referrals, and pre-market opportunities before the public feeds."

Then redeploy the edge function.