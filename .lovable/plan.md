

# Success Hub Polish — Blue Icons + Button Text

## Changes

### 1. `src/components/success-hub/MarketActivityRow.tsx`
Add `text-primary` to the three metadata icons (Bed, Bath, Square) on lines 260-270. The surrounding text stays `text-muted-foreground`.

### 2. `src/pages/success-hub/SuccessHubDashboard.tsx`
Change the empty state button label from `"Add New Listing"` to `"Add Listing"` (line ~102).

Two targeted single-line-level edits, no other files touched.

