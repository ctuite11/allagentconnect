Remove the search criteria pills (MA / status pills) from the hot sheet card on `HotSheetBuyerDetail`. Delete lines 617-619 (`<div className="mt-2"><CriteriaPills criteria={hs.criteria} /></div>`). Nothing else touched.