

# Plan: Lighten Ecosystem Section Background

## Scope
Modify only `src/components/home-v2/EcosystemSection.tsx`

## Change

**1. Lighten background**
- Change `BG_LIGHT` from `#f1f5f9` to `#f8fafc` (slate-50) — lighter, airier
- Update gradient transition band bottom to match: `#f8fafc`

**2. Increase grid visibility slightly**
- Change grid opacity from `0.04` to `0.06` so the lines read as a subtle architectural grid on the lighter surface

These are the only two changes. Everything else stays as-is.

